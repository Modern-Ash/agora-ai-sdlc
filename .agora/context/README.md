# Context

Stable contracts extracted from the epics so agents need not reread them. GitHub keeps task-specific detail. Each file lists its source issues; regenerate it if those change (see [CONTEXT template](../templates/CONTEXT.md)).
