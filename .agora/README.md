# .agora

Agent-facing assets. `templates/` holds the Markdown templates; `context/` holds stable, versioned contracts. Per-issue work goes in `execution/<issue>/`, created when an issue is implemented (not pre-created). See [task-execution](../docs/development/task-execution.md).
