# Security policy

Report vulnerabilities privately through GitHub's "Report a vulnerability" (Security tab) on this repository; do not open public issues for them. Never include credentials in reports, issues, PRs or logs. This project stores no provider credentials by design. Supported versions: the latest release on `main` while the project is pre-1.0.
