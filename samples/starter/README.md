# Starter bootstrap sample

Bootstraps a fresh temporary repository from an explicit Starter configuration after generating the same preview used by the interactive path. It installs the shipped `ai-sdlc@0.1.0` snapshot, creates accountable actors, one swarm, and the first Unit of Work without credentials or external CLIs.

```bash
uv run agora-ai-sdlc run-sample starter
```
