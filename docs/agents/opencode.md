# OpenCode

Uses the shared contract in [AGENTS.md](../../AGENTS.md) and [task-execution](../development/task-execution.md) without assuming any model. Configure the runtime and provider outside this repository; never commit credentials. Produce the same `TASK.md`, `TESTS.md`, `RESULT.md` artifacts as any other agent.
